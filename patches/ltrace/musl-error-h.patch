--- a/sysdeps/linux-gnu/mips/plt.c
+++ b/sysdeps/linux-gnu/mips/plt.c
@@ -23,7 +23,6 @@
 
 #include <string.h>
 #include <errno.h>
-#include <error.h>
 #include <gelf.h>
 #include <sys/ptrace.h>
 
--- a/zero.c
+++ b/zero.c
@@ -18,7 +18,6 @@
  * 02110-1301 USA
  */
 
-#include <error.h>
 #include <errno.h>
 
 #include "zero.h"
@@ -98,7 +97,10 @@ expr_node_zero(void)
 	if (node == NULL) {
 		node = malloc(sizeof(*node));
 		if (node == NULL)
-			error(1, errno, "malloc expr_node_zero");
+		{
+			perror("malloc expr_node_zero");
+			exit(1);
+		}
 		expr_init_cb1(node, &zero1_callback,
 			      expr_self(), 0, (void *)-1);
 	}
--- a/read_config_file.c
+++ b/read_config_file.c
@@ -27,7 +27,6 @@
 #include <stdlib.h>
 #include <ctype.h>
 #include <errno.h>
-#include <error.h>
 #include <assert.h>
 
 #include "common.h"
@@ -1259,8 +1258,11 @@ init_global_config(void)
 {
 	struct arg_type_info *info = malloc(2 * sizeof(*info));
 	if (info == NULL)
-		error(1, errno, "malloc in init_global_config");
-
+	{
+		perror("malloc in init_global_config");
+		exit(1);
+	}
+	
 	memset(info, 0, 2 * sizeof(*info));
 	info[0].type = ARGTYPE_POINTER;
 	info[0].u.ptr_info.info = &info[1];
--- a/expr.c
+++ b/expr.c
@@ -21,7 +21,6 @@
 #include <string.h>
 #include <assert.h>
 #include <errno.h>
-#include <error.h>
 #include <stdlib.h>
 
 #include "expr.h"
@@ -331,7 +330,10 @@ expr_self(void)
 	if (node == NULL) {
 		node = malloc(sizeof(*node));
 		if (node == NULL)
-			error(1, errno, "malloc expr_self");
+		{
+			perror("malloc expr_self");
+			exit(1);
+		}
 		expr_init_self(node);
 	}
 	return node;